#include "main.h"

/**
 * _islower - Checks for lowercase letters
 *
 * Return: 1 if c is lowercase otherwise return is 0
 */
int _islower(int c)
{
	int ch;

	if (ch >= 'a' && ch <= 'z')
	{
		return = 1;
	}
	else
	{
		return = 0;
	}
}
